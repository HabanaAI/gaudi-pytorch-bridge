// Autogenerated file by run_gen_op.py. Do not edit directly!

#include <torch/extension.h>
#include <pybind11/stl.h>
#include <pybind11/pybind11.h>
#include <torch/csrc/jit/tensorexpr/tensorexpr_init.h>
#include <torch/csrc/jit/python/pybind_utils.h>
#include "cpu_fallback.h"

using habana_helpers::DTypeHelper;
using namespace torch::jit;


namespace habana {

static CheckNodeWithSharedLayerValidator validator_exp_fast_math("exp_fast_math", "exp_fast_math_fwd", {0}, {}, nullptr, {}, false, false, false, false);
static CheckNodeWithSharedLayerValidator validator_ind2ptr("ind2ptr", "ind2ptr", {0}, {}, Ind2ptrMeta, {}, false, false, false, false);


struct shared_layer_exp_fast_math : SharedLayerOp {
bool func(torch::jit::Stack &stack, bool is_dynamic) {
  if (stack.size() == 1) {
    auto ivalue_arr = torch::jit::last(stack, 1);
    if (ivalue_arr[0].isTensor() ) {

      c10::IValue self = std::move(peek(stack, 0, 1));

      at::Tensor self_base = self.to<at::Tensor>();

      auto is_supported = impl(self_base, is_dynamic);
      return is_supported;
    }
  }
  return false;
}
private:
bool impl(const at::Tensor & self, bool is_dynamic) {
  VAL_RETURN_IF_UNSUPPORTED_DTYPE(exp_fast_math, is_dynamic, self)

  return true;
}

};

struct shared_layer_ind2ptr : SharedLayerOp {
bool func(torch::jit::Stack &stack, bool is_dynamic) {
  if (stack.size() == 2) {
    auto ivalue_arr = torch::jit::last(stack, 2);
    if (ivalue_arr[0].isTensor() && ivalue_arr[1].isInt() ) {

      c10::IValue ind = std::move(peek(stack, 0, 2));
      c10::IValue M = std::move(peek(stack, 1, 2));

      at::Tensor ind_base = ind.to<at::Tensor>();
      int64_t M_base = M.to<int64_t>();

      auto is_supported = impl(ind_base, M_base, is_dynamic);
      return is_supported;
    }
  }
  return false;
}
private:
bool impl(at::Tensor ind, int64_t M, bool is_dynamic) {
  VAL_RETURN_IF_UNSUPPORTED_DTYPE(ind2ptr, is_dynamic, ind, M)

  return true;
}

};





}  // namespace habana


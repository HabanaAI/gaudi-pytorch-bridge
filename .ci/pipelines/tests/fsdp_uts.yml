common:
  testRepos:
    - habanalabs
    - automation
    - pytorch-training-tests
    - pytorch-integration
    - event_tests_plugin
    - model_garden
    - pytorch-fork
    - deepspeed-fork
    - fairseq-fork
    - optimum-habana-fork
  dependencies:
    - LKD
    - HL_THUNK
    - SPECS
    - SPECS_EXTERNAL
    - SYNAPSE
    - HCL
    - SYNAPSE_PROFILER
    - SYNAPSE_UTILS
    - COMPLEX_GUID
    - CORAL_SIM
    - TPCSIM
    - TENSORFLOW_TRAINING
    - TENSORFLOW_TRAINING_TESTS
    - HOROVOD_FORK
    - TPC_KERNELS
    - CODEC
    - MEDIA
    - HABANA_REGS_CLI
    - SIVAL
    - SIVAL2
    - SWTOOLS_SDK
    - AEON
    - TPC_LLVM
    - TPC_SCALAR
    - TPC_SCALAR_KERNELS
    - KINETO
    - EVENT_TESTS
    - SPARTA
    - DEMOS
    - QUAL
    - SPECS_H9
    - RDMA_CORE
    - MULTINODE_TESTS
    - HABANAQA_FRAMEWORK_FEATURES
    - HABANAQA_CORE
    - HABANAQA_SYNAPSE
    - MODELS_ORANGE
    - HABANAQA_MEDIA
    - HABANAQA_SYNAPSE_FEATURES
    - FAIRSEQ_FORK
    - HUDA
    - NIC_KMD
    - MODEL_GARDEN
    - PYTORCH_MODULES
    - PT_FORK
    - DEEPSPEED_FORK
    - SYNAPSE_GC_TPC_TESTS
    - HABANAQA_INFERENCE
    - HABANAQA_HCCL
    - HLML
    - MEGATRON_DEEPSPEED_FORK
    - DEEPSPEED_EXAMPLES_FORK
    - TPC_FUSER
  config:
    testCommand: "${HABANA_SOFTWARE_STACK}/pytorch-training-tests/tests/torch_training_tests/scripts/run_fsdp_uts.sh"
    loadLKD: true
    testXMLOutputKey: -o
    releaseFlag: false
    saveToFileBeatMount: true
  envVars:
    LD_LIBRARY_PATH: "${LD_LIBRARY_PATH}:${HOME}/.venv/lib/${__python_cmd}/site-packages/torch/lib:$(${__python_cmd} -c 'import habana_frameworks.torch.lib; print(habana_frameworks.torch.lib.__path__[0]);')"
    CI_WORKSPACE_TO_TOX: "${WORKSPACE}"
    LD_PRELOAD: "/lib/x86_64-linux-gnu/libtcmalloc.so.4"
    JOB_SCENARIO: pt_fsdp_uts
  preTestStages:
    Install Python test dependencies:
      command: '__check_pytorch_dev_py_deps; __check_pytest_dev_py_deps'
    Update PyTorch module:
        command: 'install_pytorch_whls; python3 -m pip install --upgrade ${MEDIA_RELEASE_BUILD}/pkgs/*.whl'
testsMap:
  Test_gaudi2_asic_fsdp_uts:
    stages:
      Run fsdp uts:
        filter:
